#include "setup.h"

void Setup::clear() {}
void Setup::isValid() {}
void Setup::place(Piece placed_piece, size_t row, size_t column) {}
void Setup::remove(size_t row, size_t column) {}
void Setup::defaultSetup() {}
void Setup::setColour(Colour starting_colour) {}